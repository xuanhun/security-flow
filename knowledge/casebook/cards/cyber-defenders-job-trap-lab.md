# Job Trap Lab

## Case Metadata

- Category: `Endpoint Forensics`
- Platform: `CyberDefenders`
- Difficulty: `Medium`
- Source PDF: `https://github.com/tim-barc/ctf_writeups/blob/main/writeups/cyber_defenders_job_trap_lab.pdf`

## Why This Case Matters

Use this case as a Endpoint Forensics reference for disk-image, office-document, registry challenges.

## Input Signals

- Artifacts: disk-image, office-document, registry, windows-events
- Tools: db-browser-sqlite, evtxecmd, ftk-imager, olevba
- Techniques: browser-forensics, http-analysis, maldoc-analysis, malware-dynamic, timeline-analysis, windows-event-analysis

## First-Principles Route

- Start from artifact inventory: event logs, registry hives, browser data, file system timelines, memory, and application databases.
- Build a timeline around the suspicious user, process, file path, login, persistence item, or network connection.
- Correlate logs with file and registry evidence before trusting one artifact alone.
- Extract concrete indicators such as process names, command lines, hashes, timestamps, users, and persistence locations.

## Solve Thinking

### Step 1: analysis of a provided disk image, the investigation reconstructs the full attack lifecycle,

- Route type: `maldoc analysis`
- Why: Maldoc routes start with stream/macro extraction and decoding before behavior claims.
- Probe: Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to extract macros, streams, embedded URLs, and decoded script content.
- Tools: db-browser-sqlite, evtxecmd, ftk-imager, olevba
- Reasoning chain:
  - Recognize the goal as maldoc analysis.
  - Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to extract macros, streams, embedded URLs, and decoded script content.
  - Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Evidence rule: Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Result: `Windows registry before staging it for exfiltration.`

### Step 2: victim's system. What is the timestamp when this download activity began?

- Route type: `maldoc analysis`
- Why: Maldoc routes start with stream/macro extraction and decoding before behavior claims.
- Probe: Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to extract macros, streams, embedded URLs, and decoded script content.
- Tools: db-browser-sqlite, evtxecmd, ftk-imager, olevba
- Reasoning chain:
  - Recognize the goal as maldoc analysis.
  - Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to extract macros, streams, embedded URLs, and decoded script content.
  - Trust the result only when the extracted evidence line or field directly supports it.
- Evidence rule: Trust the result only when the extracted evidence line or field directly supports it.
- Result: `2025-07-08 08:14`

### Step 3: the system. What is the full directory path where these secondary payloads were stored?

- Route type: `maldoc analysis`
- Why: Maldoc routes start with stream/macro extraction and decoding before behavior claims.
- Probe: Use db-browser-sqlite, evtxecmd, ftk-imager, olevba with the extracted filter/query `olevba "Apply Form.docm"` and inspect the matching evidence.
- Tools: db-browser-sqlite, evtxecmd, ftk-imager, olevba
- Filters or commands:
  - `olevba "Apply Form.docm"`
- Reasoning chain:
  - Recognize the goal as maldoc analysis.
  - Use db-browser-sqlite, evtxecmd, ftk-imager, olevba with the extracted filter/query `olevba "Apply Form.docm"` and inspect the matching evidence.
  - Trust the result only when the extracted evidence line or field directly supports it.
- Evidence rule: Trust the result only when the extracted evidence line or field directly supports it.
- Result: `C:\Users\Administrator\AppData\Local\Microsoft\Windows\Update`

### Step 4: What is the creation timestamp for the scripts generated by the malicious document? (in UTC)

- Route type: `maldoc analysis`
- Why: Maldoc routes start with stream/macro extraction and decoding before behavior claims.
- Probe: Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to extract macros, streams, embedded URLs, and decoded script content.
- Tools: db-browser-sqlite, evtxecmd, ftk-imager, olevba
- Reasoning chain:
  - Recognize the goal as maldoc analysis.
  - Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to extract macros, streams, embedded URLs, and decoded script content.
  - Trust the result only when the extracted evidence line or field directly supports it.
- Evidence rule: Trust the result only when the extracted evidence line or field directly supports it.
- Result: `2025-07-08 08:23`

### Step 5: control purposes. What is the IP address of this C2 server?

- Route type: `event-log correlation`
- Why: Event-log cases should be solved by field correlation, not by reading logs chronologically.
- Probe: Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to search the relevant event IDs and correlate user, process, host, and timestamp fields.
- Tools: db-browser-sqlite, evtxecmd, ftk-imager, olevba
- Reasoning chain:
  - Recognize the goal as event-log correlation.
  - Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to search the relevant event IDs and correlate user, process, host, and timestamp fields.
  - Trust the result only when the extracted evidence line or field directly supports it.
- Evidence rule: Trust the result only when the extracted evidence line or field directly supports it.
- Result: `63.178.197.110`

### Step 6: is the complete command line that this scheduled task executes?

- Route type: `maldoc analysis`
- Why: Maldoc routes start with stream/macro extraction and decoding before behavior claims.
- Probe: Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to extract macros, streams, embedded URLs, and decoded script content.
- Tools: db-browser-sqlite, evtxecmd, ftk-imager, olevba
- Reasoning chain:
  - Recognize the goal as maldoc analysis.
  - Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to extract macros, streams, embedded URLs, and decoded script content.
  - Trust the result only when the extracted evidence line or field directly supports it.
- Evidence rule: Trust the result only when the extracted evidence line or field directly supports it.
- Result: `created:`

### Step 7: find the XML file for this Scheduled Task. Here we can see that it uses wscript to execute a file called “Updater.vbs”:

- Route type: `db-browser-sqlite-driven evidence lookup`
- Why: For Endpoint Forensics, keep every answer tied to one artifact, one probe, and one proof field.
- Probe: Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to collect the smallest evidence slice that answers the goal.
- Tools: db-browser-sqlite, evtxecmd, ftk-imager, olevba
- Reasoning chain:
  - Recognize the goal as db-browser-sqlite-driven evidence lookup.
  - Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to collect the smallest evidence slice that answers the goal.
  - Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Evidence rule: Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Result: `wscript`

### Step 8: was used to launch this subsequent script?

- Route type: `event-log correlation`
- Why: Event-log cases should be solved by field correlation, not by reading logs chronologically.
- Probe: Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to search the relevant event IDs and correlate user, process, host, and timestamp fields.
- Tools: db-browser-sqlite, evtxecmd, ftk-imager, olevba
- Reasoning chain:
  - Recognize the goal as event-log correlation.
  - Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to search the relevant event IDs and correlate user, process, host, and timestamp fields.
  - Trust the result only when the extracted evidence line or field directly supports it.
- Evidence rule: Trust the result only when the extracted evidence line or field directly supports it.
- Result: `powershell.exe -Exec Bypass`

### Step 9: scheduled task. What is the name of this secondary scheduled task?

- Route type: `http evidence extraction`
- Why: HTTP questions usually reduce to request metadata, response body, host/URI correlation, or object extraction.
- Probe: Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to inspect HTTP requests, hosts, URIs, headers, and response bodies.
- Tools: db-browser-sqlite, evtxecmd, ftk-imager, olevba
- Reasoning chain:
  - Recognize the goal as http evidence extraction.
  - Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to inspect HTTP requests, hosts, URIs, headers, and response bodies.
  - Trust the result only when the extracted evidence line or field directly supports it.
- Evidence rule: Trust the result only when the extracted evidence line or field directly supports it.
- Result: `MicrosoftEdgeUpdateTaskMachineUC`

### Step 10: Which Windows built-in utility was leveraged for this network discovery activity?

- Route type: `db-browser-sqlite-driven evidence lookup`
- Why: For Endpoint Forensics, keep every answer tied to one artifact, one probe, and one proof field.
- Probe: Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to collect the smallest evidence slice that answers the goal.
- Tools: db-browser-sqlite, evtxecmd, ftk-imager, olevba
- Reasoning chain:
  - Recognize the goal as db-browser-sqlite-driven evidence lookup.
  - Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to collect the smallest evidence slice that answers the goal.
  - Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Evidence rule: Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Result: `TRACERT`

### Step 11: What is the original name of this executable?

- Route type: `event-log correlation`
- Why: Event-log cases should be solved by field correlation, not by reading logs chronologically.
- Probe: Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to search the relevant event IDs and correlate user, process, host, and timestamp fields.
- Tools: db-browser-sqlite, evtxecmd, ftk-imager, olevba
- Reasoning chain:
  - Recognize the goal as event-log correlation.
  - Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to search the relevant event IDs and correlate user, process, host, and timestamp fields.
  - Trust the result only when the extracted evidence line or field directly supports it.
- Evidence rule: Trust the result only when the extracted evidence line or field directly supports it.
- Result: `AutoHotKey.exe`

### Step 12: is the name of the registry value entry used for this temporary storage?

- Route type: `registry artifact correlation`
- Why: Registry artifacts are strongest when paired with timestamp and user context.
- Probe: Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to inspect registry hives or parsed registry artifacts for persistence and user activity.
- Tools: db-browser-sqlite, evtxecmd, ftk-imager, olevba
- Reasoning chain:
  - Recognize the goal as registry artifact correlation.
  - Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to inspect registry hives or parsed registry artifacts for persistence and user activity.
  - Trust the result only when the extracted evidence line or field directly supports it.
- Evidence rule: Trust the result only when the extracted evidence line or field directly supports it.
- Result: `KeypressValue`

### Step 13: for staging. What is the filename of this staging file?

- Route type: `db-browser-sqlite-driven evidence lookup`
- Why: For Endpoint Forensics, keep every answer tied to one artifact, one probe, and one proof field.
- Probe: Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to collect the smallest evidence slice that answers the goal.
- Tools: db-browser-sqlite, evtxecmd, ftk-imager, olevba
- Reasoning chain:
  - Recognize the goal as db-browser-sqlite-driven evidence lookup.
  - Use db-browser-sqlite, evtxecmd, ftk-imager, olevba to collect the smallest evidence slice that answers the goal.
  - Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Evidence rule: Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Result: `logFileuyovaqv.bin`

## Reuse Checklist

- Match the new challenge's artifact and question shape against this case before copying any tool sequence.
- Start from the same evidence class, then adapt filters, fields, and timestamps to the new artifact.
- Promote any repeated manual action into a skill command before relying on it for the final solve.
