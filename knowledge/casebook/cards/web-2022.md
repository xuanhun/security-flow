# [鹏城杯 2022]简单包含

## Case Metadata

- Category: `Web`
- Platform: `鹏城杯 2022`
- Difficulty: `unknown`
- Source repository: `https://github.com/CUCCS/ctf-wps`
- Source commit: `50d7ff1080f746e3d0f23a80e22d86c0f8a67d70`
- Source file: `web/鹏城杯2022-简单包含.md`
- Source URL: `https://github.com/CUCCS/ctf-wps/blob/50d7ff1080f746e3d0f23a80e22d86c0f8a67d70/web/%E9%B9%8F%E5%9F%8E%E6%9D%AF2022-%E7%AE%80%E5%8D%95%E5%8C%85%E5%90%AB.md`
- Local source mirror: `sources/cuccs-ctf-wps/repo/web/鹏城杯2022-简单包含.md`
- Challenge URL: `https://www.nssctf.cn/problem/2422`

## Why This Case Matters

Use this case as a Web reference for ciphertext, web-app challenges.

## Input Signals

- Artifacts: ciphertext, web-app
- Tools: hackbar, cyberchef, netcat
- Techniques: classical-crypto, command-injection, encoding-analysis, file-inclusion, http-analysis, ret2libc, waf-bypass, web-exploitation

## First-Principles Route

- Identify the exposed entry point, reflected behavior, trust boundary, and any explicit hints or blacklists.
- Validate the suspected primitive with the smallest payload that proves code/data/control influence.
- Chain the primitive into file read, code execution, auth bypass, or state manipulation only after recording the bypass condition.

## Linked Assets

- Referenced assets: `3`
- `web/<images/[鹏城杯 2022]简单包含-waf.jpg>`
- `web/<images/[鹏城杯 2022]简单包含-index_php.jpg>`
- `web/<images/[鹏城杯 2022]简单包含-get_flag.jpg>`

## Solve Thinking

### Step 1: 看到什么

- Route type: `file inclusion exploitation`
- Why: Inclusion cases usually start with path control and end with wrapper, traversal, or source disclosure pivots.
- Probe: Use hackbar, cyberchef, netcat to verify controllable include/read paths and then test the shortest local or wrapper-based read primitive.
- Tools: hackbar, cyberchef, netcat
- Reasoning chain:
  - Recognize the section as file inclusion exploitation.
  - Use hackbar, cyberchef, netcat to verify controllable include/read paths and then test the shortest local or wrapper-based read primitive.
  - Trust the result only when the extracted evidence line or field directly supports it.
- Evidence rule: Trust the result only when the extracted evidence line or field directly supports it.
- Result: `flag.php`

### Step 2: 想到什么解题思路

- Route type: `file inclusion exploitation`
- Why: Inclusion cases usually start with path control and end with wrapper, traversal, or source disclosure pivots.
- Probe: Use hackbar, cyberchef, netcat to verify controllable include/read paths and then test the shortest local or wrapper-based read primitive.
- Tools: hackbar, cyberchef, netcat
- Reasoning chain:
  - Recognize the section as file inclusion exploitation.
  - Use hackbar, cyberchef, netcat to verify controllable include/read paths and then test the shortest local or wrapper-based read primitive.
  - Trust the result only when the extracted evidence line or field directly supports it.
- Evidence rule: Trust the result only when the extracted evidence line or field directly supports it.
- Result: `flag.php`

### Step 3: 尝试过程和结果记录

- Route type: `waf bypass`
- Why: WAF cases are best solved by isolating one blocked token at a time and preserving a working bypass pattern.
- Probe: Use hackbar, cyberchef, netcat to isolate the blocked token, then vary encoding, transport, syntax, or indirection until the primitive survives filtering.
- Tools: hackbar, cyberchef, netcat
- Reasoning chain:
  - Recognize the section as waf bypass.
  - Use hackbar, cyberchef, netcat to isolate the blocked token, then vary encoding, transport, syntax, or indirection until the primitive survives filtering.
  - Trust the result only when the extracted evidence line or field directly supports it.
- Evidence rule: Trust the result only when the extracted evidence line or field directly supports it.
- Result: `flag.php`

### Step 4: 尝试多种编码方式绕过关键词检测

- Route type: `file inclusion exploitation`
- Why: Inclusion cases usually start with path control and end with wrapper, traversal, or source disclosure pivots.
- Probe: Use netcat to verify controllable include/read paths and then test the shortest local or wrapper-based read primitive.
- Tools: netcat
- Reasoning chain:
  - Recognize the section as file inclusion exploitation.
  - Use netcat to verify controllable include/read paths and then test the shortest local or wrapper-based read primitive.
  - Trust the result only when the extracted evidence line or field directly supports it.
- Evidence rule: Trust the result only when the extracted evidence line or field directly supports it.
- Result: `flag.php`

### Step 5: php_filter_chain_generator

- Route type: `http evidence extraction`
- Why: HTTP questions usually reduce to request metadata, response body, host/URI correlation, or object extraction.
- Probe: Use hackbar, cyberchef, netcat to inspect HTTP requests, hosts, URIs, headers, and response bodies.
- Tools: hackbar, cyberchef, netcat
- Reasoning chain:
  - Recognize the section as http evidence extraction.
  - Use hackbar, cyberchef, netcat to inspect HTTP requests, hosts, URIs, headers, and response bodies.
  - Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Evidence rule: Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Result: ````bash`

### Step 6: 生成执行 `system("id")` 的过滤器链

- Route type: `command execution path`
- Why: Command execution routes should separate syntax proof, filter bypass, and final file-read or shell step.
- Probe: Use hackbar, cyberchef, netcat with the extracted filter/query `python3 php_filter_chain_generator.py --chain '<?=system("id");?>'` and inspect the matching evidence.
- Tools: hackbar, cyberchef, netcat
- Filters or commands:
  - `python3 php_filter_chain_generator.py --chain '<?=system("id");?>'`
- Reasoning chain:
  - Recognize the section as command execution path.
  - Use hackbar, cyberchef, netcat with the extracted filter/query `python3 php_filter_chain_generator.py --chain '<?=system("id");?>'` and inspect the matching evidence.
  - Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Evidence rule: Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Result: `python3 php_filter_chain_generator.py --chain '<?=system("id");?>'`

## Reuse Checklist

- Match the new challenge's artifact and question shape against this case before copying any tool sequence.
- Start from the same evidence class, then adapt filters, fields, and timestamps to the new artifact.
- Promote any repeated manual action into a skill command before relying on it for the final solve.
