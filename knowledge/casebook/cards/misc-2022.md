# **[羊城杯 2022]躲猫猫**

## Case Metadata

- Category: `Misc`
- Platform: `羊城杯`
- Difficulty: `unknown`
- Source repository: `https://github.com/CUCCS/ctf-wps`
- Source commit: `50d7ff1080f746e3d0f23a80e22d86c0f8a67d70`
- Source file: `misc/羊城杯_2022_躲猫猫.md`
- Source URL: `https://github.com/CUCCS/ctf-wps/blob/50d7ff1080f746e3d0f23a80e22d86c0f8a67d70/misc/%E7%BE%8A%E5%9F%8E%E6%9D%AF_2022_%E8%BA%B2%E7%8C%AB%E7%8C%AB.md`
- Local source mirror: `sources/cuccs-ctf-wps/repo/misc/羊城杯_2022_躲猫猫.md`

## Why This Case Matters

Use this case as a Misc reference for ciphertext, pcap, web-app challenges.

## Input Signals

- Artifacts: ciphertext, pcap, web-app
- Tools: binwalk, netcat, radare2, wireshark
- Techniques: crypto-analysis, http-analysis, misc-analysis, network-forensics, qr-analysis, stego-extraction, traffic-analysis, web-exploitation

## First-Principles Route

- Classify the artifact first: encoding, media, archive, QR, traffic, document, or mixed puzzle.
- Use the cheapest extraction or decoding pass that can confirm the intended modality before branching out.
- Treat each recovered clue as a pivot into the next artifact instead of assuming the first visible string is final.

## Linked Assets

- Referenced assets: `14`
- `misc/images/羊城杯_2022_躲猫猫/protocal.png`
- `misc/images/羊城杯_2022_躲猫猫/binwalk.png`
- `misc/images/羊城杯_2022_躲猫猫/offset.png`
- `misc/images/羊城杯_2022_躲猫猫/zip.png`
- `misc/images/羊城杯_2022_躲猫猫/tls.png`
- `misc/images/羊城杯_2022_躲猫猫/miss_cat.png`
- `misc/images/羊城杯_2022_躲猫猫/missing_cat.png`
- `misc/images/羊城杯_2022_躲猫猫/tcp29.png`
- `misc/images/羊城杯_2022_躲猫猫/key_0.png`
- `misc/images/羊城杯_2022_躲猫猫/key_1.png`
- `misc/images/羊城杯_2022_躲猫猫/key.png`
- `misc/images/羊城杯_2022_躲猫猫/cat.png`
- ... and `2` more

## Solve Thinking

### Step 1: 考点

- Route type: `cipher decoding`
- Why: Crypto cases should peel encodings first and keep every transformation individually checkable.
- Probe: Use binwalk, netcat, radare2, wireshark to separate encoding cleanup from the actual cipher break and verify the plaintext structure after each step.
- Tools: binwalk, netcat, radare2, wireshark
- Reasoning chain:
  - Recognize the section as cipher decoding.
  - Use binwalk, netcat, radare2, wireshark to separate encoding cleanup from the actual cipher break and verify the plaintext structure after each step.
  - Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Evidence rule: Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Result: `#流量分析 #TLS解密 #Maxicode`

### Step 2: 思路

- Route type: `cipher decoding`
- Why: Crypto cases should peel encodings first and keep every transformation individually checkable.
- Probe: Use binwalk, wireshark with the extracted filter/query `zipinfo payload.zip | head` and inspect the matching evidence.
- Tools: binwalk, wireshark
- Filters or commands:
  - `zipinfo payload.zip | head`
- Reasoning chain:
  - Recognize the section as cipher decoding.
  - Use binwalk, wireshark with the extracted filter/query `zipinfo payload.zip | head` and inspect the matching evidence.
  - Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Evidence rule: Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Result: `#!/usr/bin/env python3`

### Step 3: -*- coding: utf-8 -*-

- Route type: `netcat-driven evidence lookup`
- Why: For Misc, keep every answer tied to one artifact, one probe, and one proof field.
- Probe: Use netcat to collect the smallest evidence slice that answers the goal.
- Tools: netcat
- Reasoning chain:
  - Recognize the section as netcat-driven evidence lookup.
  - Use netcat to collect the smallest evidence slice that answers the goal.
  - Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Evidence rule: Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Result: `def decrypt_image(enc_path: str, secret_path: str, out_path: str):`

### Step 4: 读取 x,y

- Route type: `binwalk-driven evidence lookup`
- Why: For Misc, keep every answer tied to one artifact, one probe, and one proof field.
- Probe: Use binwalk, netcat, radare2, wireshark to collect the smallest evidence slice that answers the goal.
- Tools: binwalk, netcat, radare2, wireshark
- Reasoning chain:
  - Recognize the section as binwalk-driven evidence lookup.
  - Use binwalk, netcat, radare2, wireshark to collect the smallest evidence slice that answers the goal.
  - Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Evidence rule: Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Result: `x, y = load_xy(secret_path)`

### Step 5: 注意：round(...,16) 会返回 float

- Route type: `binwalk-driven evidence lookup`
- Why: For Misc, keep every answer tied to one artifact, one probe, and one proof field.
- Probe: Use binwalk, netcat, radare2, wireshark to collect the smallest evidence slice that answers the goal.
- Tools: binwalk, netcat, radare2, wireshark
- Reasoning chain:
  - Recognize the section as binwalk-driven evidence lookup.
  - Use binwalk, netcat, radare2, wireshark to collect the smallest evidence slice that answers the goal.
  - Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Evidence rule: Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Result: `u3 = (y * 3680) / x`

### Step 6: 读入加密图像（RGB 三通道）

- Route type: `cipher decoding`
- Why: Crypto cases should peel encodings first and keep every transformation individually checkable.
- Probe: Use netcat to separate encoding cleanup from the actual cipher break and verify the plaintext structure after each step.
- Tools: netcat
- Reasoning chain:
  - Recognize the section as cipher decoding.
  - Use netcat to separate encoding cleanup from the actual cipher break and verify the plaintext structure after each step.
  - Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Evidence rule: Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Result: `h, w, _ = c.shape`

### Step 7: 解密输出

- Route type: `cipher decoding`
- Why: Crypto cases should peel encodings first and keep every transformation individually checkable.
- Probe: Use binwalk, netcat, radare2, wireshark to separate encoding cleanup from the actual cipher break and verify the plaintext structure after each step.
- Tools: binwalk, netcat, radare2, wireshark
- Reasoning chain:
  - Recognize the section as cipher decoding.
  - Use binwalk, netcat, radare2, wireshark to separate encoding cleanup from the actual cipher break and verify the plaintext structure after each step.
  - Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Evidence rule: Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Result: `p = np.zeros_like(c, dtype=np.uint8)`

### Step 8: 逐像素复现 logistic 序列并解密

- Route type: `cipher decoding`
- Why: Crypto cases should peel encodings first and keep every transformation individually checkable.
- Probe: Use binwalk, netcat, radare2, wireshark to separate encoding cleanup from the actual cipher break and verify the plaintext structure after each step.
- Tools: binwalk, netcat, radare2, wireshark
- Reasoning chain:
  - Recognize the section as cipher decoding.
  - Use binwalk, netcat, radare2, wireshark to separate encoding cleanup from the actual cipher break and verify the plaintext structure after each step.
  - Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Evidence rule: Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Result: `for j in range(w):`

### Step 9: 与原脚本一致的迭代顺序

- Route type: `radare2-driven evidence lookup`
- Why: For Misc, keep every answer tied to one artifact, one probe, and one proof field.
- Probe: Use radare2 with the extracted filter/query `r2 = int(x2 * 255)` and inspect the matching evidence.
- Tools: radare2
- Filters or commands:
  - `r2 = int(x2 * 255)`
- Reasoning chain:
  - Recognize the section as radare2-driven evidence lookup.
  - Use radare2 with the extracted filter/query `r2 = int(x2 * 255)` and inspect the matching evidence.
  - Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Evidence rule: Treat the extracted answer as proven only after locating the source artifact field that produced it.
- Result: `r3 = int(x3 * 255)`

### Step 10: 用 int 计算避免 uint8 下溢问题

- Route type: `cipher decoding`
- Why: Crypto cases should peel encodings first and keep every transformation individually checkable.
- Probe: Use netcat to separate encoding cleanup from the actual cipher break and verify the plaintext structure after each step.
- Tools: netcat
- Reasoning chain:
  - Recognize the section as cipher decoding.
  - Use netcat to separate encoding cleanup from the actual cipher break and verify the plaintext structure after each step.
  - Trust the result only when the extracted evidence line or field directly supports it.
- Evidence rule: Trust the result only when the extracted evidence line or field directly supports it.
- Result: `- flag：NSSCTF{ozqgVLol1DvK8giNVdvGslr_aZKKwNuv_q-FzqB5N3hHHqn3}`

## Reuse Checklist

- Match the new challenge's artifact and question shape against this case before copying any tool sequence.
- Start from the same evidence class, then adapt filters, fields, and timestamps to the new artifact.
- Promote any repeated manual action into a skill command before relying on it for the final solve.
