# Migrated Case Workspace

This file records the prior quality workspace migration. Reusable validation
cases now live directly under `cases/`, durable review notes live under
`cases/reports/`, and generated run artifacts live under each
`engagements/<id>/` directory.
